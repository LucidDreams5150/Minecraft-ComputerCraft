--Custom Basalt Theme for farmControl
return { -- The default main theme for basalt!
    BaseFrameBG = colors.gray,
    BaseFrameText = colors.orange,
    FrameBG = colors.gray,
    FrameText = colors.orange,
    ButtonBG = colors.gray,
    ButtonText = colors.orange,
    CheckboxBG = colors.gray,
    CheckboxText = colors.orange,
    InputBG = colors.orange,
    InputText = colors.gray,
    TextfieldBG = colors.orange,
    TextfieldText = colors.white,
    ListBG = colors.purple,
    ListText = colors.orange,
    MenubarBG = colors.gray,
    MenubarText = colors.orange,
    DropdownBG = colors.black,
    DropdownText = colors.orange,
    RadioBG = colors.gray,
    RadioText = colors.orange,
    SelectionBG = colors.orange,
    SelectionText = colors.white,
    GraphicBG = colors.orange,
    ImageBG = colors.orange,
    PaneBG = colors.orange,
    ProgramBG = colors.orange,
    ProgressbarBG = colors.gray,
    ProgressbarText = colors.orange,
    ProgressbarActiveBG = colors.orange,
    ScrollbarBG = colors.gray,
    ScrollbarText = colors.gray,
    ScrollbarSymbolColor = colors.orange,
    SliderBG = false,
    SliderText = colors.gray,
    SliderSymbolColor = colors.orange,
    SwitchBG = colors.gray,
    SwitchText = colors.gray,
    LabelBG = false,
    LabelText = colors.orange,
    GraphBG = colors.gray,
    GraphText = colors.orange,
}